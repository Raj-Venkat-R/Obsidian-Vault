---
title: Java ArrayList – Important Functions
topic: Data Structures
subtopic: Dynamic Arrays
purpose: Quick Reference for LeetCode
language: Java
---

# 📋 Java `ArrayList` – Important Functions Table

## 🔹 Creation

| Function | Description | Example |
|--------|------------|---------|
| `new ArrayList<>()` | Creates an empty ArrayList | `ArrayList<Integer> list = new ArrayList<>();` |
| `new ArrayList<>(n)` | Creates ArrayList with initial capacity | `new ArrayList<>(10)` |

---

## 🔹 Add Elements

| Function | Description | Example |
|--------|------------|---------|
| `add(E e)` | Add element at end | `list.add(10);` |
| `add(int index, E e)` | Insert element at index | `list.add(1, 20);` |

---

## 🔹 Access Elements

| Function | Description | Example |
|--------|------------|---------|
| `get(int index)` | Get element at index | `int x = list.get(0);` |

---

## 🔹 Update Elements

| Function | Description | Example |
|--------|------------|---------|
| `set(int index, E e)` | Update element at index | `list.set(1, 99);` |

---

## 🔹 Remove Elements

| Function | Description | Example |
|--------|------------|---------|
| `remove(int index)` | Remove element by index | `list.remove(2);` |
| `remove(Object o)` | Remove element by value | `list.remove(Integer.valueOf(10));` |

⚠️ Removing shifts remaining elements automatically.

---

## 🔹 Size & State

| Function | Description | Example |
|--------|------------|---------|
| `size()` | Returns number of elements | `int s = list.size();` |
| `isEmpty()` | Checks if list is empty | `list.isEmpty();` |

---

## 🔹 Search Operations

| Function | Description | Example |
|--------|------------|---------|
| `contains(Object o)` | Checks if element exists | `list.contains(30);` |
| `indexOf(Object o)` | Returns first index of element | `list.indexOf(20);` |
| `lastIndexOf(Object o)` | Returns last index | `list.lastIndexOf(20);` |

---

## 🔹 Traversal

| Method | Description | Example |
|------|------------|---------|
| For loop | Index-based traversal | `for(int i=0;i<list.size();i++)` |
| Enhanced for-loop | Value-based traversal | `for(int x : list)` |

---

## 🔹 Utility Operations

| Function | Description | Example |
|--------|------------|---------|
| `clear()` | Removes all elements | `list.clear();` |
| `toArray()` | Converts to array | `list.toArray();` |
| `clone()` | Shallow copy | `list.clone();` |

---

## 🔹 Comparison with Arrays (Quick Reminder)

| Feature | Array | ArrayList |
|------|------|-----------|
| Size | Fixed | Dynamic |
| Resize | ❌ | ✅ |
| Methods | ❌ | ✅ |
| Syntax | Simple | Rich API |

---

## 🔹 Core Reminder (VERY IMPORTANT)

> `ArrayList` internally uses an **array**  
> These functions hide **shifting & resizing logic**

---



